# 🛡️ Website Security Guidelines
**School It Research & Development Organization (SIRDO)**  
_সুরক্ষিত ওয়েবসাইট বজায় রাখার নির্দেশিকা_

---

## 🔐 ১. HTTPS সক্রিয় রাখুন
- সবসময় ওয়েবসাইটটি `https://` ব্যবহার করবে।  
- GitHub Pages ব্যবহার করলে SSL সার্টিফিকেট স্বয়ংক্রিয়ভাবে যুক্ত থাকে।  
- HTTP থেকে HTTPS রিডাইরেক্ট নিশ্চিত করুন।

---

## 🧱 ২. ফাইল পারমিশন ও অ্যাক্সেস কন্ট্রোল
- HTML, CSS, JS ফাইলগুলোতে **read-only (644)** পারমিশন দিন।  
- **public write (777)** পারমিশন ব্যবহার করবেন না।  
- অ্যাডমিন বা config ফোল্ডার প্রাইভেট রাখুন।

---

## 🔒 ৩. অ্যাডমিন এরিয়া প্রোটেকশন
যদি অ্যাডমিন প্যানেল থাকে:
- শক্তিশালী পাসওয়ার্ড ব্যবহার করুন (অক্ষর + সংখ্যা + সিম্বল)।  
- `.htaccess` ও `.htpasswd` ফাইল দিয়ে ফোল্ডার প্রটেক্ট করতে পারেন।

---

## ⚙️ ৪. ইনপুট ভ্যালিডেশন ও XSS প্রোটেকশন
- ইউজারের ইনপুট সবসময় **sanitize** করুন।  
- `innerHTML` ব্যবহার না করে `textContent` বা `innerText` ব্যবহার করুন।  
- ফর্ম সাবমিশনের সময় HTTPS ব্যবহার করুন।

---

## 🧠 ৫. জাভাস্ক্রিপ্ট ও থার্ড-পার্টি স্ক্রিপ্ট সিকিউরিটি
- শুধুমাত্র নির্ভরযোগ্য সোর্স (CDNJS, jsDelivr, Google CDN) ব্যবহার করুন।  
- অপরিচিত সোর্সের স্ক্রিপ্ট বা কোড ব্যবহার করবেন না।

---

## 🧍‍♂️ ৬. ইউজার ডেটা সুরক্ষা
- ইউজার ইনফরমেশন সংগ্রহ করলে তা **encrypt** করে সংরক্ষণ করুন।  
- ডেটা **শুধু প্রয়োজনীয় পরিমাণেই সংগ্রহ করুন**।

---

## 🧰 ৭. ব্যাকআপ ও রিস্টোর
- ওয়েবসাইট কোড এবং ডেটার **সাপ্তাহিক ব্যাকআপ** রাখুন।  
- GitHub ব্যবহার করলে এটি স্বয়ংক্রিয়ভাবে version control রাখে।

---

## 🧹 ৮. অপ্রয়োজনীয় ফাইল মুছে ফেলুন
- test.html, demo.php, readme.txt ইত্যাদি ফাইল ডিলিট করুন।  
- পুরনো কোড বা ব্যাকআপ ফাইল কখনও public ফোল্ডারে রাখবেন না।

---

## 🧩 ৯. Content Security Policy (CSP)
`<head>` ট্যাগে নিচের মেটা ট্যাগ যুক্ত করুন:
```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; img-src *; script-src 'self' https://trustedcdn.com;">
